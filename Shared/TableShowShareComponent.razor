@using InsureManage.Models;
<table class="table-primary">
    <tr>
        <td>ไอดี</td>
        <td>ชื่อตำแหน่งที่เก็บของ</td>
    </tr>
    @foreach (var v in PositionContent)
    {
        <tr>
            <td>@v.IdPosition</td>
            <td>@v.NamePosition</td>
        </tr>
    }
</table>
@code {
    [CascadingParameter(Name = "PositionContent")]
    private List<Position> PositionContent { get; set; } = null;
}